# Minimal placeholder Dockerfile for frontend service
# This allows docker-compose to parse successfully when bringing up other services.
FROM alpine:3.19
WORKDIR /app
CMD ["sh", "-c", "echo 'Frontend placeholder container'; sleep infinity"]
